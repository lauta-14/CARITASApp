package com.example.caritas.ui.confirmar

import androidx.lifecycle.ViewModel

class ConfirmarCompraViewModel : ViewModel() {
    // En este caso, no necesitamos almacenar datos en este ViewModel,
    // ya que la confirmación de la compra se maneja en el fragmento mismo.
}
